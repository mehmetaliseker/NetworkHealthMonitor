<UserControl x:Class="NetworkHealthMonitor.Views.Shell.MainShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:shell="clr-namespace:NetworkHealthMonitor.Views.Shell">
    <Grid Background="{StaticResource ShellBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{Binding IsNavigationCollapsed, Converter={StaticResource NavigationWidthConverter}}" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <shell:SidebarNavigation Grid.Column="0" />

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <shell:TopBar Grid.Row="0" />

            <Border Grid.Row="1" Padding="22,18,22,10">
                <ContentControl Content="{Binding CurrentPage}"
                                KeyboardNavigation.TabNavigation="Continue" />
            </Border>

            <shell:ToastHost Grid.Row="1"
                             HorizontalAlignment="Right"
                             VerticalAlignment="Bottom"
                             Margin="0,0,28,26" />

            <Border Grid.Row="2"
                    BorderBrush="{StaticResource ShellBorderBrush}"
                    BorderThickness="0,1,0,0"
                    Background="{StaticResource ShellSurfaceBrush}"
                    Padding="18,8">
                <DockPanel LastChildFill="False">
                    <TextBlock DockPanel.Dock="Left"
                               Text="{Binding StatusMessage}"
                               Foreground="{StaticResource ShellMutedTextBrush}"
                               TextTrimming="CharacterEllipsis"
                               MaxWidth="780" />
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                        <TextBlock Text="UI: " Foreground="{StaticResource ShellSubtleTextBrush}" />
                        <TextBlock Text="{Binding UiStatusText}" Margin="0,0,14,0" />
                        <TextBlock Text="Servis: " Foreground="{StaticResource ShellSubtleTextBrush}" />
                        <TextBlock Text="{Binding WorkerHealthText}" />
                    </StackPanel>
                </DockPanel>
            </Border>

            <shell:GlobalDialogHost Grid.RowSpan="3" />
        </Grid>
    </Grid>
</UserControl>
