<UserControl x:Class="NetworkHealthMonitor.Views.Reports.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer Style="{StaticResource PageScrollStyle}">
        <StackPanel>
            <ItemsControl ItemsSource="{Binding Legacy.ReportOptions}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding}"
                                Command="{Binding DataContext.Legacy.SelectReportCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                CommandParameter="{Binding}"
                                Style="{StaticResource SecondaryToolbarButtonStyle}"
                                MinWidth="190"
                                AutomationProperties.Name="{Binding}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Border Style="{StaticResource PanelSectionStyle}" Margin="0,12,0,0">
                <WrapPanel>
                    <DatePicker SelectedDate="{Binding Legacy.ReportStartDate}" Width="150" AutomationProperties.Name="Rapor baslangic tarihi" />
                    <DatePicker SelectedDate="{Binding Legacy.ReportEndDate}" Width="150" AutomationProperties.Name="Rapor bitis tarihi" />
                    <ComboBox ItemsSource="{Binding Legacy.DeviceOptions}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding Legacy.ReportDeviceFilterId}" Width="220" Style="{StaticResource ModernComboBoxStyle}" AutomationProperties.Name="Rapor cihaz filtresi" />
                    <ComboBox ItemsSource="{Binding Legacy.DeviceGroupOptions}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding Legacy.ReportGroupFilterId}" Width="220" Style="{StaticResource ModernComboBoxStyle}" AutomationProperties.Name="Rapor grup filtresi" />
                    <Button Content="Raporu Olustur" Command="{Binding Legacy.RefreshAvailabilityCommand}" Style="{StaticResource ToolbarButtonStyle}" AutomationProperties.Name="Raporu olustur" />
                    <Button Content="CSV Disa Aktar" Command="{Binding Legacy.ExportAvailabilityCommand}" Style="{StaticResource SecondaryToolbarButtonStyle}" AutomationProperties.Name="Raporu CSV disa aktar" />
                </WrapPanel>
            </Border>

            <TextBlock Text="{Binding Legacy.SelectedReportName}" Style="{StaticResource SectionHeadingStyle}" Margin="0,12,0,0" />
            <DataGrid ItemsSource="{Binding Legacy.AvailabilityItems}" Style="{StaticResource CompactDataGridStyle}" ColumnHeaderStyle="{StaticResource ModernDataGridHeaderStyle}" MaxHeight="360">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Cihaz" Binding="{Binding DeviceName}" Width="1.6*" />
                    <DataGridTextColumn Header="IP / Hostname" Binding="{Binding IpAddress}" Width="150" />
                    <DataGridTextColumn Header="Grup" Binding="{Binding GroupName}" Width="150" />
                    <DataGridTextColumn Header="30 gun" Binding="{Binding Availability30DaysText}" Width="110" />
                    <DataGridTextColumn Header="Durum" Binding="{Binding CurrentAvailabilityStatusText}" Width="140" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
